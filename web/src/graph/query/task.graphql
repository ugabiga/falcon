query GetTask ($tradingAccountID: ID!) {
    tasks(tradingAccountId: $tradingAccountID){
        id
        tradingAccountID
        cron
        nextExecutionTime
        isActive
        type
    }
}