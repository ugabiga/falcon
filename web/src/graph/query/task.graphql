query GetTaskIndex($tradingAccountID: ID){
    taskIndex(tradingAccountID: $tradingAccountID){
        selectedTradingAccount {
            id
            name
            exchange
            ip
            identifier
            tasks {
                id
                cron
                type
                nextExecutionTime
                isActive
                tradingAccountID
            }
        }
        tradingAccounts {
            id
            name
            exchange
            ip
            identifier
            tasks {
                id
                cron
                type
                nextExecutionTime
                isActive
                tradingAccountID
            }
        }
    }
}

mutation CreateTask($tradingAccountID: ID!, $days: String!, $hours: String!, $type: String!){
    createTask(tradingAccountID: $tradingAccountID, days: $days hours: $hours type: $type){
        id
        cron
        nextExecutionTime
        isActive
    }
}

mutation UpdateTask($id: ID!, $days: String! $hours: String! $type: String! $isActive: Boolean!){
    updateTask(id: $id, days: $days, hours: $hours type: $type isActive: $isActive){
        id
        cron
        nextExecutionTime
        isActive
    }
}