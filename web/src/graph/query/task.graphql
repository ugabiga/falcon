query GetTaskIndex($tradingAccountID: ID){
    taskIndex(tradingAccountID: $tradingAccountID){
        selectedTradingAccount {
            id
            exchange
            currency
            ip
            identifier
            tasks {
                id
                cron
                type
                nextExecutionTime
                isActive
            }
        }
        tradingAccounts {
            id
            exchange
            currency
            ip
            identifier
            tasks {
                id
                cron
                type
                nextExecutionTime
                isActive
            }
        }
    }
}

mutation CreateTask($tradingAccountID: ID!, $cron: String! $type: String!){
    createTask(tradingAccountID: $tradingAccountID, cron: $cron type: $type){
        id
        cron
        nextExecutionTime
        isActive
    }
}
