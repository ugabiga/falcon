query GetTaskIndex($tradingAccountID: ID){
    taskIndex(tradingAccountID: $tradingAccountID){
        selectedTradingAccount {
            id
            name
            exchange
            ip
            identifier
            tasks {
                id
                currency
                cron
                type
                nextExecutionTime
                isActive
                tradingAccountID
            }
        }
        tradingAccounts {
            id
            name
            exchange
            ip
            identifier
            tasks {
                id
                currency
                cron
                type
                nextExecutionTime
                isActive
                tradingAccountID
            }
        }
    }
}

mutation CreateTask($tradingAccountID: ID!, $currency: String!, $days: String!, $hours: String!, $type: String!){
    createTask(tradingAccountID: $tradingAccountID, currency: $currency days: $days hours: $hours type: $type){
        id
        currency
        cron
        nextExecutionTime
        isActive
    }
}

mutation UpdateTask($id: ID!, $currency: String!, $days: String! $hours: String! $type: String! $isActive: Boolean!){
    updateTask(id: $id, currency: $currency, days: $days, hours: $hours type: $type isActive: $isActive){
        id
        currency
        cron
        nextExecutionTime
        isActive
    }
}