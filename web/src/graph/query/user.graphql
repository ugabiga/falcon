query GetUser {
    user {
        id
        name
        timezone
    }
}

mutation UpdateUser (
    $name: String!
    $timezone: String!
) {
    updateUser(input: {
        name: $name
        timezone: $timezone
    }) {
        id
        name
        timezone
    }
}