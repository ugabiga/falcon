extend type Query {
    taskHistoryIndex(taskID: ID!): TaskHistoryIndex!
}

type TaskHistoryIndex{
    taskHistories: [TaskHistory!]
}

type TaskHistory {
    id: ID!
    taskID: ID!
    isSuccess: Boolean!
    updatedAt: Time!
    createdAt: Time!
    task: Task!
}
